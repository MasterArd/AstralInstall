# AstralInstall

_Easily install and update GitHub projects,_ built on Go and C++

## explenation:

C++ handles the frontend and user requests and sends that to the Go backend which (is gonna) read that from `stdin` and send the response through `stdout` using JSON